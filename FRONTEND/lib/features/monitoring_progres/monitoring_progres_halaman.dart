import 'package:flutter/material.dart';

class MonitoringProgresHalaman extends StatelessWidget {
  const MonitoringProgresHalaman({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Halaman Statistik Progres',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
