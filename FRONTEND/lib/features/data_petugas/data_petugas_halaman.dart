import 'package:flutter/material.dart';

class DataPetugasHalaman extends StatelessWidget {
  const DataPetugasHalaman({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Halaman Tabel Data Petugas',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
